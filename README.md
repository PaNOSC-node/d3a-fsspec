The program's purpose is to access datasets based on a given DOI via Metalink parsing. It works as follows:

- When running, the program will ask to input a DOI

```python prefix = "" Please input the DOI:```
